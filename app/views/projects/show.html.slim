.project
  .sidebar
    = @project.first_release_date
    = @project.licence
    = @project.technologies
    = @project.third_parties
    = @project.acknowledgements
  .info
    h1 = @project.title
    - if @project.incomplete?
      .state
        .triangle
          p !!
        ' incomplete


    - if @project.project_url
      .reference
        = @project.address

    .description = [@project.title, ' '].join(' ') * 100
